@AGENTS.md

The file above is the single operating contract for this repository —
product boundaries, truth map, invariants, and workflow all live there.
Tool-session notes only:

- The Stop hook in `.claude/settings.json` follows the Automation
  policy in AGENTS.md: warn only, never change Git or publish.
- The FlightTrackerLED workspace habits in `~/CLAUDE.md`
  (docs-autoupdate, release-sync) do **not** apply to this repo — it is
  clean-room and has no customer site content to sync.
